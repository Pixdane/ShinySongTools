[package]
name = "core"
description = "Platform primitives for the Shiny Song Tools runtime: thread capabilities, gates, method-pointer slots, callback-safe containers, compact events, and the plugin error chain."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"

[lib]
name = "corelib"

[dependencies]
bevy_ecs = { workspace = true }
crossbeam-queue = { workspace = true }
crossbeam-utils = { workspace = true }
il2cpp-bridge-rs = { workspace = true }
libc = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
